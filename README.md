# cameraSnapshotsMap

warehouse camera map and snapshots

## SVG 地图文件说明

- 当前页面会加载 `static/demoMap.svg` 作为仓库地图底图。
- 你可以用 Inkscape 直接编辑并覆盖这个文件，不需要改 `templates/index.html`。
- 摄像头点位采用比例坐标（`x_ratio`, `y_ratio`），地图缩放后不会偏移。
